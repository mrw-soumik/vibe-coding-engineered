# MVPFlow AI dependencies.
#
# Versions are PINNED to the set verified working for the workshop. A fresh
# install previously pulled newer FastAPI/slowapi that broke the /workflow
# endpoint and the demo, so pin deliberately and re-test before bumping.

# Core
fastapi==0.115.14
uvicorn[standard]==0.30.6
pydantic==2.12.5
pydantic-settings==2.5.2

# Database
sqlalchemy==2.0.44
alembic==1.13.3

# Authentication & Security
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 can't read bcrypt>=4.1 version metadata (breaks hashing)
python-dotenv==1.2.1

# Rate Limiting
slowapi==0.1.9

# LLM (Claude) integration, optional; the workflow falls back to deterministic
# templates when no ANTHROPIC_API_KEY is set or the SDK is absent.
anthropic==0.75.0

# HTTP client (used at runtime by the Jira integration; also used by tests)
httpx==0.28.1

# Testing
pytest==9.0.1
pytest-asyncio==1.3.0

# Production
gunicorn==26.0.0
psycopg2-binary==2.9.11  # PostgreSQL support

# Monitoring & Observability (optional)
# datadog>=0.46.0
# sentry-sdk>=1.40.0

# Development
black==26.5.1
isort==8.0.1
flake8==7.3.0
mypy==2.1.0
